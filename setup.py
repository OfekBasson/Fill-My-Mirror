from setuptools import setup, find_packages

setup(
    name="fill_my_mirror",
    version="0.1",
    description="Fill My Mirror: Geometry-aware mirror completion",
    packages=find_packages(),
    python_requires=">=3.9",
)